---
description: Create an execution plan for a task
argument-hint: "<task>"
---
Create an execution plan for: $ARGUMENTS

Requirements:
- Break the work into concrete, ordered steps.
- Call out affected packages/files in this monorepo.
- Include validation steps (checks/tests) for each part.
- Keep scope minimal and avoid unrelated changes.
